**CREATE NEW BRANCH**
    git checkout -b branchName

**ADD FILE**
    git add filename/.

**COMMIT**
    git commit -m "commit message"

**PUSH**
    git push origin branchName

**CLONE REPO**
    git clone gitReooURL